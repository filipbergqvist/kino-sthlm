<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the window frame before Compose draws; the app's real colours live in Theme.kt.
         Split day/night so there is no white flash launching in dark mode. -->
    <style name="Theme.KinoSthlm" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
